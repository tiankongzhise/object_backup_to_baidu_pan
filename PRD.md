# 产品需求文档 (PRD)

## object_backup_to_baidu_pan - 百度网盘备份系统

| 版本 | 日期 | 描述 |
|------|------|------|
| 1.3 | 2026-01-17 | 分离敏感配置到.env |
| 1.2 | 2026-01-17 | hash_service已完成，更新待办事项 |
| 1.1 | 2026-01-17 | 根据更新后的README完善需求 |
| 1.0 | 2026-01-17 | 初始版本 |

---

## 一、项目概述

### 1.1 项目背景

本程序是一个备份系统，用于将本地文件和文件夹加密打包后备份到百度网盘（百度网盘）。是 `item_backup_simple` 的 v2 版本。

### 1.2 核心功能

| 功能 | 描述 |
|------|------|
| 文件打包 | 将文件夹下的一级子文件和文件夹加密打包 |
| 去重备份 | 比较本地文件与数据库记录，已备份文件不重复上传 |
| 完整性验证 | 压缩包解压验证，确保备份正确性 |
| 断点续传 | 支持失败重试和断点续传 |
| 资源控制 | 磁盘空间可控，避免不足 |
| 监控日志 | 提供监控指标和详细日志 |

### 1.3 技术栈

| 类别 | 技术选型 |
|------|----------|
| 语言 | Python 3.13+ |
| 包管理 | uv |
| 文件路径 | pathlib |
| 配置管理 | TOML (配置) + .env (敏感信息) |
| 数据库 | MySQL + SQLAlchemy ORM 2.0 + PyMySQL |
| ZIP处理 | pyzipper (AES加密) |
| 云存储 | 百度网盘 OpenAPI |
| Hash算法 | MD5, SHA1, SHA256 |

---

## 二、核心流程设计

### 2.1 整体处理流程

```
┌─────────────────────────────────────────────────────────────────────┐
│                        文件扫描阶段                                  │
├─────────────────────────────────────────────────────────────────────┤
│  扫描源文件夹 → 获取一级子文件/文件夹列表                            │
│        ↓                                                            │
│  与数据库比对 → 计算文件Hash (MD5/SHA1/SHA256)                      │
│        ↓                                                            │
│  标记需要备份的文件 → 标记已备份的文件（直接删除）                   │
├─────────────────────────────────────────────────────────────────────┤
│                        同步处理阶段                                  │
├─────────────────────────────────────────────────────────────────────┤
│  对需要备份的文件/文件夹 → 加密打包为ZIP                             │
│        ↓                                                            │
│  ZIP文件计算Hash → 记录到数据库（避免重复计算）                      │
│        ↓                                                            │
│  解压验证 → 比对解压后文件Hash与源文件Hash一致                       │
├─────────────────────────────────────────────────────────────────────┤
│                        异步上传阶段                                  │
├─────────────────────────────────────────────────────────────────────┤
│  上传任务入队列 → 后台线程处理                                       │
│        ↓                                                            │
│  上传成功回调 → 数据库更新 → 删除源文件和压缩包                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 2.2 文件处理规则

#### 2.2.1 打包范围

- **输入**：用户指定的源文件夹
- **处理对象**：该文件夹下的一级子文件和一级子文件夹
- **打包方式**：每个一级子项单独打包为一个ZIP文件
- **压缩级别**：ZIP压缩包本身不再压缩（存储模式）

#### 2.2.2 去重规则

- **已备份判断**：比较本地文件的 MD5/SHA1/SHA256 与数据库记录
- **去重逻辑**：三种Hash都一致才认为是同一文件
- **已备份文件处理**：直接删除源文件，不重复上传

#### 2.2.3 特殊标注（人工处理）

| 条件 | 处理方式 |
|------|----------|
| 文件/文件夹大小 > 20GB | 跳过，标记为需要人工处理 |
| 一级子文件夹包含文件数 > 200 | 跳过，标记为需要人工处理 |

### 2.3 Hash计算规范

#### 2.3.1 源文件Hash

- 算法：MD5、SHA1、SHA256
- 判定标准：三种Hash全部一致才认为是同一文件
- 用途：文件去重、完整性验证

#### 2.3.2 压缩包Hash

- 计算时机：压缩完成后
- 记录位置：数据库
- 用途：避免重复计算压缩包的Hash

### 2.4 验证机制

#### 2.4.1 解压验证流程

1. 对生成的ZIP压缩包进行解压
2. 计算解压出来的文件/文件夹的Hash
3. 与源文件/源文件夹的Hash进行比对
4. 一致则验证通过，不一致则重新压缩

---

## 三、存储路径规范

### 3.1 路径配置

由配置文件指定以下路径的父级目录：
- 压缩文件目录
- 解压文件目录
- 百度云盘目标目录

**约束**：压缩文件与解压文件路径需保持在同一个磁盘，否则报错。

### 3.2 路径生成规则

| 类型 | 路径格式 |
|------|----------|
| 本地ZIP | `{压缩目录}/YYYYMMDD/{源文件夹名}/解压密码_{password}/{文件名}.zip` |
| 百度云盘 | `{百度云盘指定目录}/YYYYMMDD/{源文件夹名}/解压密码_{password}/{文件名}.zip` |
| 解压目录 | `{解压目录}/YYYYMMDD/{源文件夹名}/解压密码_{password}/{文件名}` |

### 3.3 路径解析工具

使用 `service/upload_service/utils.py` 中的 `extract_date_and_password_from_path()` 函数解析路径，提取日期（YYYYMMDD）和解压密码。

---

## 四、资源管理策略

### 4.1 磁盘空间管理

#### 监控阈值
- 默认限制：80GB（可通过配置调整）
- 计算方式：压缩文件指定目录所在盘的可用磁盘空间
- 初始化检查：启动时判断剩余空间，不够直接报错退出

#### 动态控制流程
1. 计算下一个待处理文件所需空间
2. 如果剩余空间不足，等待已上传任务释放空间
3. 空间释放后检查是否满足下一个文件需求

### 4.2 各阶段空间占用估算

| 阶段 | 操作 | 额外空间占用 |
|------|------|-------------|
| 1 | 源文件Hash计算 | 0 |
| 2 | 压缩处理 | 压缩包大小 |
| 3 | 解压验证 | 压缩包大小 + 解压文件大小 |
| 4 | 验证完成 | 压缩包大小 |
| 5 | 上传完成 | 0 |

---

## 五、异步上传机制

### 5.1 架构设计

```
┌──────────────┐     ┌──────────────┐     ┌──────────────┐
│   处理线程    │ --> │  上传队列    │ --> │  上传线程    │
│ (压缩/验证)   │     │ (Queue)      │     │ (异步上传)   │
└──────────────┘     └──────────────┘     └──────────────┘
                                                  │
                    ┌─────────────────────────────┘
                    ↓
              ┌──────────────┐
              │   回调处理    │
              ├──────────────┤
              │ - 数据库更新  │
              │ - 清理文件    │
              └──────────────┘
```

### 5.2 功能特性

- 上传任务放入队列，后台线程处理
- 上传完成触发回调函数
- 支持失败重试
- 支持断点续传

---

## 六、系统组件设计

### 6.1 控制流程

```python
while True:
    # 1. 检查磁盘空间
    if disk_used < 80GB - next_file_required_space:
        # 2. 获取下一个待处理文件
        next_file = file_queue.get()

        if not next_file:
            break

        # 3. 计算所需空间
        required_space = calculate_required_space(next_file)

        # 4. 预留空间，开始处理
        with space_reservation(required_space):
            process_single_file(next_file)

        # 5. 文件进入上传队列
        upload_queue.put(processed_file)
    else:
        # 等待空间释放
        wait_for_space()

    # 6. 后台上传线程处理上传队列
    process_upload_queue_async()
```

### 6.2 核心模块

| 模块 | 职责 |
|------|------|
| 文件扫描器 | 扫描源文件夹，获取一级子项，计算Hash，与数据库比对 |
| 压缩服务 | ZIP压缩，支持AES加密，不对ZIP再次压缩 |
| 验证服务 | 解压验证压缩包完整性，比对Hash |
| 上传服务 | 百度网盘API对接，分片上传 |
| 队列管理器 | 管理上传队列，处理回调 |
| 空间管理器 | 监控磁盘空间，动态控制 |
| 数据库服务 | SQLAlchemy ORM，记录文件Hash和任务状态 |
| 人工标记服务 | 处理大小>20G或文件数>200的特殊情况 |

---

## 七、错误处理和恢复

### 7.1 异常处理策略

| 场景 | 处理方式 |
|------|----------|
| 压缩失败 | 重试3次后标记为失败 |
| 验证失败 | 删除压缩包，重新压缩 |
| 上传失败 | 加入重试队列，最多重试5次 |
| 空间不足 | 暂停处理，等待空间释放 |
| 文件大小 > 20GB | 标记为人工处理，跳过 |
| 文件数 > 200 | 标记为人工处理，跳过 |

### 7.2 恢复机制

- 定期检查未完成的任务
- 记录各阶段状态，支持断点续做
- 清理僵尸进程和临时文件

---

## 八、监控和日志

### 8.1 监控指标

| 指标 | 描述 |
|------|------|
| 当前磁盘使用量 | 压缩目录所在盘的使用量 |
| 待处理文件队列长度 | 等待处理的文件数量 |
| 上传队列长度 | 等待上传的文件数量 |
| 各阶段处理时间 | 扫描、压缩、上传各阶段耗时 |
| 成功率/失败率 | 各阶段的成功和失败比例 |
| 人工标记数量 | 需要人工处理的文件数 |
| 去重数量 | 跳过重复备份的文件数 |

### 8.2 日志记录

- 每个文件的完整处理轨迹
- Hash计算结果
- 空间使用情况变化
- 异常和错误详情
- 性能统计信息
- 人工标记原因

---

## 九、部署建议

### 9.1 性能优化

| 配置项 | 建议值 | 说明 |
|--------|--------|------|
| 并发数 | 1-2 | 同时处理的文件数量 |
| 上传分片大小 | 根据网络状况调整 | 默认20MB |
| Hash计算 | 多线程并行 | 加快扫描速度 |

### 9.2 定期维护

- 清理临时文件
- 清理过期日志
- 检查数据库记录

---

## 十、环境要求

### 10.1 开发环境

```bash
# 使用uv管理项目
uv sync              # 安装依赖
uv run <command>     # 运行命令
uv add <package>     # 添加依赖
```

### 10.2 测试环境

| 环境 | 路径 |
|------|------|
| 测试文件来源 | D:\测试用例 |
| 测试目录 | D盘测试文件夹 |

### 10.3 文件路径规范

- 所有文件路径使用 `pathlib` 管理

### 10.4 配置管理

- 配置文件格式：TOML
- 敏感信息：`.env` 文件
- 数据库：MySQL
- ORM：SQLAlchemy 2.0 风格

### 10.5 依赖清单

| 包名 | 用途 |
|------|------|
| pyzipper | ZIP压缩与解压（AES加密） |
| python-dotenv | .env 环境变量读取 |
| sqlalchemy | 数据库ORM |
| pymysql | MySQL连接 |
| tomllib | TOML配置读取 |

---

## 十一、待办事项

### 11.1 已完成功能

| 功能 | 状态 | 位置 |
|------|------|------|
| Hash计算服务 | ✅ 完成 | `service/hash_service/` |
| ZIP压缩/解压服务 | ✅ 完成 | `service/zip_service.py` |
| 百度网盘上传服务 | ✅ 完成 | `service/upload_service/` |

### 11.2 已知问题（来自README）

- [ ] 检查zip服务的路径是否符合规范
- [ ] `main.py` 为空，需要实现主入口
- [ ] 数据库模型尚未定义
- [ ] 配置文件模块尚未实现
- [ ] 解压验证逻辑尚未实现（ZipService已有unzip_item）
- [ ] 人工标记功能（大小>20G、文件数>200）尚未实现
- [ ] 分类服务尚未实现（与HashConfig、ClassifyConfig关联）
- [ ] 配置模块尚未实现（HashConfig、ClassifyConfig、ZipConfig）

---

## 十二、配置示例

| 配置类型 | 存储位置 | 说明 |
|----------|----------|------|
| 敏感信息 | `.env` 文件 | 数据库密码、百度Token、邮箱密码等 |
| 应用配置 | `config.toml` | 路径、限制参数、连接池配置等 |
| 数据库配置 | `mysql.env` | 数据库连接信息（已废弃，请使用 .env） |

### 12.1 config.toml (应用配置)

```toml
[source]
path = "/path/to/source"
password = ""  # ZIP加密密码，可选

[storage]
compress_dir = "/path/to/compress"
extract_dir = "/path/to/extract"
disk_limit_gb = 80

[limits]
max_file_size_gb = 20
max_files_per_folder = 200

[compression]
level = 0  # 存储模式，不压缩

[upload]
chunk_size_mb = 20
retry_times = 5

[database]
pool_size = 5
max_overflow = 10
pool_timeout = 30
pool_recycle = 3600
echo = false
```

### 12.2 .env (敏感信息)

```env
# 数据库连接
MYSQL_HOST=localhost
MYSQL_PORT=3306
MYSQL_USER=root
MYSQL_PASSWORD=your_password
MYSQL_DATABASE=backup_db

# 百度网盘
BAIDU_PAN_ACCESS_TOKEN=your_access_token
BAIDU_PAN_REFRESH_TOKEN=your_refresh_token
BAIDU_PAN_APP_KEY=your_app_key
BAIDU_PAN_SECRET_KEY=your_secret_key

# 邮件通知 (可选)
SMTP_HOST=smtp.163.com
SMTP_PORT=465
SMTP_USER=your_email@163.com
SMTP_PASSWORD=your_password
```

---

## 十三、数据库设计

### 13.1 表结构设计

#### 13.1.1 source_files - 源文件信息表

| 字段 | 类型 | 描述 |
|------|------|------|
| id | BIGINT | 主键 |
| file_path | VARCHAR(1024) | 源文件完整路径 |
| file_name | VARCHAR(255) | 文件名 |
| file_size | BIGINT | 文件大小（字节） |
| md5_hash | CHAR(32) | MD5哈希 |
| sha1_hash | CHAR(40) | SHA1哈希 |
| sha256_hash | CHAR(64) | SHA256哈希 |
| is_backup | BOOLEAN | 是否已备份 |
| backup_time | DATETIME | 备份时间 |
| created_at | DATETIME | 创建时间 |
| updated_at | DATETIME | 更新时间 |

#### 13.1.2 backup_packages - 备份压缩包表

| 字段 | 类型 | 描述 |
|------|------|------|
| id | BIGINT | 主键 |
| package_path | VARCHAR(1024) | 本地ZIP文件路径 |
| baidu_pan_path | VARCHAR(1024) | 百度云盘路径 |
| source_file_id | BIGINT | 关联的源文件ID |
| md5_hash | CHAR(32) | ZIP文件MD5 |
| sha1_hash | CHAR(40) | ZIP文件SHA1 |
| sha256_hash | CHAR(64) | ZIP文件SHA256 |
| password | VARCHAR(64) | ZIP解压密码 |
| file_count | INT | 包内文件数量 |
| package_size | BIGINT | 包大小 |
| status | VARCHAR(20) | 状态：pending/uploading/completed/failed |
| created_at | DATETIME | 创建时间 |
| uploaded_at | DATETIME | 上传完成时间 |

#### 13.1.3 upload_records - 上传记录表

| 字段 | 类型 | 描述 |
|------|------|------|
| id | BIGINT | 主键 |
| package_id | BIGINT | 关联的备份包ID |
| baidu_file_id | VARCHAR(255) | 百度云盘文件ID |
| upload_status | VARCHAR(20) | 状态 |
| retry_count | INT | 重试次数 |
| error_message | TEXT | 错误信息 |
| created_at | DATETIME | 创建时间 |
| completed_at | DATETIME | 完成时间 |

#### 13.1.4 manual_review_items - 人工审核表

| 字段 | 类型 | 描述 |
|------|------|------|
| id | BIGINT | 主键 |
| file_path | VARCHAR(1024) | 文件/文件夹路径 |
| file_size | BIGINT | 文件大小 |
| file_count | INT | 子文件数量（文件夹） |
| reason | VARCHAR(100) | 原因：size_exceeded/count_exceeded |
| status | VARCHAR(20) | 状态：pending/processed |
| created_at | DATETIME | 创建时间 |

#### 13.1.5 operation_logs - 操作日志表

| 字段 | 类型 | 描述 |
|------|------|------|
| id | BIGINT | 主键 |
| operation_type | VARCHAR(50) | 操作类型 |
| file_path | VARCHAR(1024) | 关联文件 |
| status | VARCHAR(20) | 状态 |
| message | TEXT | 日志信息 |
| duration_ms | INT | 耗时（毫秒） |
| created_at | DATETIME | 创建时间 |

---

## 十四、验收标准

- [ ] 正确扫描源文件夹，获取一级子文件和文件夹
- [ ] 正确计算文件的 MD5/SHA1/SHA256 三种Hash
- [ ] 与数据库比对，正确识别已备份文件
- [ ] 已备份文件直接删除，不重复上传
- [ ] 文件大小 > 20GB 时正确标记为人工处理
- [ ] 文件夹包含 > 200 个文件时正确标记为人工处理
- [ ] 成功创建AES加密的ZIP压缩包（不再次压缩）
- [ ] 压缩包Hash记录到数据库
- [ ] 解压验证通过，计算Hash与源文件一致
- [ ] 成功上传到百度网盘
- [ ] 完成后正确清理本地源文件和压缩包
- [ ] 磁盘空间不足时正确处理
- [ ] 失败情况正确重试
- [ ] 记录完整的处理日志和Hash信息
- [ ] 测试用例在 D:\测试用例 运行通过
